"$schema" = "schemas/schema.json"

[default]
description = "webview 캡처·분석 명령(snapshot / record / set_occlusion / analyze_regions) 허용."
permissions = [
    "allow-snapshot",
    "allow-record",
    "allow-set-occlusion",
    "allow-analyze-regions",
    "allow-analyze-frame-diffs",
]
